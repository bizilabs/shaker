name: Publishing

on:
  push:
    tags:
      - '*'

concurrency:
  group: ${{ github.workflow }} - ${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout project sources
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'gradle'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          cache-read-only: false

      - name: Build and package distribution
        run: ./gradlew packageDistributionForCurrentOS
        env:
          VERSION: ${{ github.ref_name }}

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: shaker-${{ github.ref_name }}-${{ matrix.os }}
          path: |
            ${{ matrix.os == 'macos-latest' && 'build/compose/binaries/main/dmg/*.dmg' }}
            ${{ matrix.os == 'ubuntu-latest' && 'build/compose/binaries/main/deb/*.deb' }}
            ${{ matrix.os == 'windows-latest' && 'build/compose/binaries/main/msi/*.msi' }}

  publish:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout project sources
        uses: actions/checkout@v4

      - name: Download Linux artifact
        uses: actions/download-artifact@v4
        with:
          name: shaker-${{ github.ref_name }}-ubuntu-latest

      - name: Download macOS artifact
        uses: actions/download-artifact@v4
        with:
          name: shaker-${{ github.ref_name }}-macos-latest

      - name: Download Windows artifact
        uses: actions/download-artifact@v4
        with:
          name: shaker-${{ github.ref_name }}-windows-latest

      - name: Publish artifacts to GitHub Releases
        uses: ncipollo/release-action@v1
        with:
          token: ${{ secrets.ACCESS_TOKEN }}
          artifacts: |
            build/compose/binaries/ubuntu-latest/*.deb
            build/compose/binaries/macos-latest/*.dmg
            build/compose/binaries/windows-latest/*.exe
          tag: ${{ github.ref_name }}
          body: |
            This release includes builds for Linux (.deb), macOS (.dmg), and Windows (.exe).
